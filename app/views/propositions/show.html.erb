<div class="right-link">
  <%= link_to "Return to All Propositions", propositions_path %>
</div>

<h1>Proposition Arguments</h1>

<%= render @proposition %>

<script>
  var allPercentages = [<%= @proposition.percentage_agreement.round %>];
</script>

<%= render partial: "proposition_tree" %>

<div class="responses">
  <% @proposition.claims.direct.each do |c| %>
    <%= render c %>
  <% end %>
</div>
